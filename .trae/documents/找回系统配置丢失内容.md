# 找回系统配置丢失内容

## 问题分析
之前的修改中，Sidebar.tsx 和 App.tsx 丢失了以下内容：
- Sidebar.tsx 系统配置菜单缺少：AI 能力配置、全局日志
- App.tsx 缺少路由：`system/ai-config`、`system/logs` 以及对应 import

## 实施步骤

### 步骤1：恢复 Sidebar.tsx 菜单
- **文件**：`src/components/Sidebar.tsx`
- **改动**：在系统配置 children 中恢复 `AI 能力配置` 和 `全局日志` 两个子项

### 步骤2：恢复 App.tsx 路由
- **文件**：`src/App.tsx`
- **改动**：
  - 恢复 import `AIConfigPanel` 和 `SystemLogs`
  - 恢复路由 `<Route path="system/ai-config" element={<AIConfigPanel />} />`
  - 恢复路由 `<Route path="system/logs" element={<SystemLogs />} />`

### 步骤3：验证 TypeScript 编译
