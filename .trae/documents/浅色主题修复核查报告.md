# 浅色主题修复核查报告

## 核查时间
2026-05-16（第三轮核查）

## 核查方法
全局搜索所有 `.tsx` 文件中的 `text-gray-100`, `text-gray-200`, `text-gray-300`, `text-gray-400`，逐一检查是否有正确的 `dark:` 前缀。

---

## ❌ 发现的遗留问题

### 问题A: 4个页面的 h1 标题仍使用硬编码 `text-gray-100`（无 dark 前缀）

**影响：浅色模式下页面标题几乎不可见！**

| 文件 | 行号 | 当前代码 | 应改为 |
|------|------|---------|--------|
| **Tasks.tsx** | L98 | `text-gray-100` | `text-gray-900 dark:text-gray-100` |
| **Brands.tsx** | L75 | `text-gray-100` | `text-gray-900 dark:text-gray-100` |
| **Customers.tsx** | L81 | `text-gray-100` | `text-gray-900 dark:text-gray-100` |
| **Projects.tsx** | L99 | `text-gray-100` | `text-gray-900 dark:text-gray-100` |

### 问题B: 4个页面的表格数据使用硬编码 `text-gray-200`（无 dark 前缀）

**影响：浅色模式下表格内容几乎不可见！**

| 文件 | 行号 | 当前代码 | 应改为 |
|------|------|---------|--------|
| **Tasks.tsx** | L140 | `text-gray-200` (table-cell) | `text-gray-800 dark:text-gray-200` |
| **Brands.tsx** | L103 | `text-gray-200` (table-cell) | `text-gray-800 dark:text-gray-200` |
| **Customers.tsx** | L114 | `text-gray-200` (table-cell) | `text-gray-800 dark:text-gray-200` |
| **Projects.tsx** | L137 | `text-gray-200` (span) | `text-gray-800 dark:text-gray-200` |

### 问题C: 旧版 shadcn/ui pagination 组件未更新

| 文件 | 行号 | 当前代码 | 状态 |
|------|------|---------|------|
| **ui/pagination.tsx** | L10 | `bg-gray-800 hover:bg-gray-700 text-gray-300` | ⚠️ 仅深色模式样式 |
| **ui/pagination.tsx** | L12 | `hover:bg-gray-800 hover:text-gray-100` | ⚠️ 仅深色模式样式 |

> 注：此文件可能未被引用（项目使用了自定义 Pagination 组件），但为安全起见应一并修复。

---

## ✅ 已正确修复的部分（验证通过）

### h1/h2/h3 标题（24处）✅
以下文件标题已全部正确使用 `text-gray-900 dark:text-gray-100`：
- TaskDetail, ImageGenerationHistory, GenerationHistory, ImageGeneration, VideoGeneration, Assets, Shots, Roles, Dashboard, Reviews, Briefs, KeyFrames, Settings

### 字段标签（15+处 TaskDetail）✅
全部使用 `text-gray-700 dark:text-gray-400`

### 图标颜色 ✅
- Search图标: `text-gray-500 dark:text-gray-400` ✅
- Edit/Eye操作图标: `text-gray-600 dark:text-gray-400` ✅
- Film/Image/ImagePlus图标: `text-gray-600 dark:text-gray-400` ✅

### 正文/字段值 ✅
- 全部使用 `text-gray-800 dark:text-gray-200` ✅

### 辅助文字 ✅
- 日期/空状态/描述: `text-gray-500/600 dark:text-gray-400/500` ✅

### shadcn/ui 组件 ✅
- button, input, select, textarea, dialog, label, table, tabs, toaster, switch, checkbox, badge, progress, avatar, tooltip — 全部已支持双主题

### 导航组件 ✅
- Sidebar: 导航项 `gray-700`, 子菜单 `gray-600` ✅
- Header: 面包屑 `gray-600`, Search/Bell图标 `gray-500` ✅

---

## 修复方案

仅需修改 **4个文件 + 1个可选文件**：

### 必须修复（4个文件，8处）
1. **src/pages/projects/Tasks.tsx** - L98 h1标题 + L140 表格单元格
2. **src/pages/projects/Brands.tsx** - L75 h1标题 + L103 表格单元格
3. **src/pages/projects/Customers.tsx** - L81 h1标题 + L114 表格单元格
4. **src/pages/projects/Projects.tsx** - L99 h1标题 + L137 span文字

### 可选修复（1个文件）
5. **src/components/ui/pagination.tsx** - 添加浅色主题支持

---

## 总结

| 类别 | 总数 | 已修复 | 遗漏 | 完成率 |
|------|------|--------|------|--------|
| 页面 h1 标题 | 19个页面 | 15 | **4** | 79% |
| 表格数据文字 | ~20处 | ~16 | **4** | 80% |
| shadcn/ui 组件 | 17个 | 16 | 1(可选) | 94% |
| 图标颜色 | ~30处 | ~30 | 0 | 100% |
| 字段标签 | ~20处 | ~20 | 0 | 100% |
| 导航/布局 | 全部 | 全部 | 0 | 100% |

**整体完成率: ~95%**

遗漏原因：这4个项目页面（Tasks/Brands/Customers/Projects）在第一轮替换时只做了表单部分的重写（Modal内的Input/Select等），但**页面顶部的h1标题和表格行中的数据文字**没有被Task agent覆盖到。
