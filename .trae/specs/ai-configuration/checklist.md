- [x] AIConfig 类型定义包含 general/video/image 三大模块及所有端点配置
- [x] getDefaultAIConfig() 返回的默认值与现有硬编码值一致
- [x] aiConfigService 提供 getAIConfig、saveAIConfig、resetAIConfig 方法
- [x] aiConfigService 提供 getVideoReqKey 和 getImageReqKey 方法
- [x] aiConfigStore 实现 loadConfig、updateGeneral、updateVideoEndpoint、updateImageEndpoint 方法
- [x] aiConfigStore 实现 resetToDefaults、exportConfig、importConfig 方法
- [x] aiConfigStore 初始化时自动从 localStorage 加载配置
- [x] videoGeneration.ts 不再使用 VIDEO_API_CONFIG 硬编码常量
- [x] videoGeneration.ts 的 getReqKeyForMode 从 aiConfigStore 读取
- [x] imageGeneration.ts 不再使用 IMAGE_API_CONFIG 中的 BASE_URL、POLL_INTERVAL、EXPIRY
- [x] imageGeneration.ts 保留 model_names 和 model_versions 映射
- [x] AIConfigPanel 组件展示通用 AI 配置（BASE_URL、apiKey、appId、轮询、过期）
- [x] AIConfigPanel 组件展示 3 个视频生成 API 端点配置
- [x] AIConfigPanel 组件展示 5 个图片生成 API 端点配置（textToImage、stylizationEdit、superResolution、inpainting + imageToImage 复用 textToImage）
- [x] AIConfigPanel 支持启用/禁用单个 API 端点
- [x] AIConfigPanel 支持导入 JSON 配置并校验格式
- [x] AIConfigPanel 支持导出当前配置为 JSON 下载
- [x] AIConfigPanel 支持重置为默认配置（二次确认）
- [x] AIConfigPanel 支持多环境预设切换
- [x] 表单校验覆盖：baseUrl URL 格式、timeout 范围、pollInterval 范围、reqKey 非空
- [x] Settings.tsx 使用 Tabs 分为通用设置、外观设置、AI 能力配置三个选项卡
- [x] Settings.tsx 保留原有所有功能（主题切换、语言、每页条数等）
- [x] TypeScript 编译零错误
- [x] 开发服务器启动无控制台错误
- [x] 视频和图片生成任务提交功能正常（服务层 API 签名不变）
